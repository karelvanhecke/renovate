---
name: Renovate config

on:
  pull_request:
    branches:
      - main

jobs:
  renovate-config-validator:
    name: Renovate config validator
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/renovatebot/renovate:44.71.2@sha256:4a38e24411bf154cf6b40c342b4c473a274ac81539137a3d4ef57e2a6a8bf1b8
      # github hosted runners are running as `1001:127` (ubuntu:docker)
      options: -u 1001:0 --group-add 1000 --group-add 12021 --group-add 127
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Validate global configuration
        run: renovate-config-validator .github/renovate-global.json
